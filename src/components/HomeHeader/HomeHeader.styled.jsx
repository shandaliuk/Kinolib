import styled from 'styled-components';
import backgroundImage from 'images/home-header.jpg';

export const Header = styled.header`
  background-image: url(${backgroundImage});
  padding-top: 40px;
  padding-bottom: 92px;
`;

export const Icon = styled.svg`
  width: 24px;
  height: 24px;
`;
